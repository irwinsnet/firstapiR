^.*\.Rproj$          # Ignores RStudio project files.
^\.Rproj\.user$      # Ignores RStudio project files.
^\.git               # Ignores git repository files.
^data-raw            # Ignores files used to create data files.
^docs$               # Ignores docs folder, which contains github pages.
